'use strict';

/**
 * @ngdoc function
 * @name yapp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of yapp
 */
angular.module('rac')
  .controller('ReportesListarCtrl', function(perfilService,$scope, $state) {

    $scope.$state = $state;
  });
